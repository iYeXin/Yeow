# 发布配置：复制为 .env 并填入实际值（.env 已被 .gitignore 排除，不会入库）
YEOW_PUBLISH_API_KEY=your-secret-key
YEOW_PUBLISH_URL=http://your-server:17492/deploy
# 各语言站点的部署路径（配置到即发布；`npm run publish` 会对每个已配置站点各发一次
# POST <YEOW_PUBLISH_URL>?path=<该站点路径>，携带各自 dist 的 zip）
CN_PATH=
EN_PATH=
